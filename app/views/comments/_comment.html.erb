<div>
  <p><strong> <%= comment.name %> </strong></p>
  <p><%= comment.body %></p>
  <p><%= comment.created_at.strftime("%dth %B, %Y %H:%M") %></p>
  <p><%= link_to 'Delete', [comment.post, comment],
        method: :delete,
        data: {confirm: "Are you sure?"} %>
  </p>
</div>